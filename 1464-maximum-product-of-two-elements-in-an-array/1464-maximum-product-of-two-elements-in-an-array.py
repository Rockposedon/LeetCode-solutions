class Solution:
    def maxProduct(self, nums: List[int]) -> int:
        
        # sort the array in descending order to get two greatest integer 
        nums.sort(reverse=True)
       
        # iterate in array
        for i in range(0,len(nums)):
            
            
            return (nums[0]-1)*(nums[1]-1)
            
        